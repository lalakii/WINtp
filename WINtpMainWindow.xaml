<Window x:Class="WINtpMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="WINtp 设置 - " 
        MinHeight="350"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="2,5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="TextAlignment" Value="Right"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="Margin" Value="2,5"/>
                <Setter Property="Padding" Value="0,2"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="2,5"/>
                <Setter Property="Padding" Value="0,2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="Grid">
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="ComboBox">
                <Setter Property="Margin" Value="2,5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="10,6"/>
                <Setter Property="MinHeight" Value="28"/>
                <Setter Property="MinWidth" Value="110"/>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Margin" Value="4,5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="系统时间: " x:Name="sysTimeTbk"  Grid.Row="0" Grid.Column="0" HorizontalAlignment="Right"/>
            <wfi:WindowsFormsHost Grid.Row="0" Grid.Column="1">
                <wf:DateTimePicker x:Name="currentTimeDpk" Format="Custom" />
            </wfi:WindowsFormsHost>
        </Grid>
        <GroupBox Grid.Column="0" Grid.Row="1" Header="服务器列表" x:Name="serverListTbk" Grid.RowSpan="8">
            <DataGrid x:Name="serverListDgd" Grid.RowSpan="20"  MaxWidth="280" CanUserReorderColumns="False" AutoGenerateColumns="False" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="域名" x:Name="domainDgc"  Binding="{Binding Url}" MinWidth="150" />
                    <DataGridComboBoxColumn Header="协议" SelectedValueBinding="{Binding Type}" MinWidth="80" x:Name="typeListCbx"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <GroupBox Grid.Column="1" Grid.Row="1" Grid.RowSpan="7" x:Name="configTbk" Header="软件配置">
            <StackPanel>
                <CheckBox Content="自动同步时间" Grid.Column="1" Grid.Row="2" x:Name="autoSyncCbx"/>
                <CheckBox Content="使用SSL" Grid.Column="1" Grid.Row="3" x:Name="useSslCbx"/>
                <CheckBox Content="日志输出" Grid.Column="1" Grid.Row="4" x:Name="logPrintCbx"/>
                <Grid Grid.Column="1" Grid.Row="5" Grid.RowSpan="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="网络超时: " x:Name="netTimeoutTbk" Grid.Row="0" Grid.Column="0"/>
                    <wfi:WindowsFormsHost Grid.Row="0" Grid.Column="1">
                        <wf:NumericUpDown x:Name="timeoutNud"/>
                    </wfi:WindowsFormsHost>
                    <TextBlock Text="毫秒" Grid.Row="0" Grid.Column="2" x:Name="millsecondTbk0"/>
                    <TextBlock Text="程序超时: " x:Name="timeoutTbk"  Grid.Row="1" Grid.Column="0"/>
                    <wfi:WindowsFormsHost Grid.Row="1" Grid.Column="1">
                        <wf:NumericUpDown x:Name="netTimeoutNud"/>
                    </wfi:WindowsFormsHost>
                    <TextBlock Text="毫秒" Grid.Row="1" Grid.Column="2" x:Name="millsecondTbk1"/>
                    <TextBlock Text="同步周期: " x:Name="intervalTbk"  Grid.Row="2" Grid.Column="0"/>
                    <wfi:WindowsFormsHost Grid.Row="2" Grid.Column="1">
                        <wf:NumericUpDown x:Name="timeSyncNud"/>
                    </wfi:WindowsFormsHost>
                    <TextBlock Text="秒" HorizontalAlignment="Left" Grid.Row="2" Grid.Column="2" x:Name="secondTbk"/>
                </Grid>
            </StackPanel>
        </GroupBox>
        <ScrollViewer Margin="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Grid.Column="1" Grid.Row="8">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <WrapPanel Orientation="Horizontal" MaxWidth="280" Grid.Column="0" Grid.Row="0">
                    <Button Content="安装服务" x:Name="installBtn" HorizontalAlignment="Center" />
                    <Button Content="保存配置" x:Name="applyBtn" HorizontalAlignment="Center" />
                    <Button Content="卸载服务" x:Name="uninstallBtn" HorizontalAlignment="Center"/>
                    <Button Content="退出程序" x:Name="exitBtn" HorizontalAlignment="Center"/>
                </WrapPanel>
                <Grid Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="2,5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock  x:Name="moreTbk" Text="获取帮助: " Grid.Row="0" Grid.Column="0"/>
                    <TextBlock Grid.Column="1" Grid.Row="0" TextAlignment="Left" Padding="4,2">
                    <Hyperlink NavigateUri="https://wintp.sourceforge.io" x:Name="homeUri">https://wintp.sourceforge.io</Hyperlink>
                    <LineBreak/>
                    <Hyperlink NavigateUri="https://github.com/lalakii/WINtp" x:Name="homeUri2">https://github.com/lalakii/WINtp</Hyperlink>
                    </TextBlock>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
